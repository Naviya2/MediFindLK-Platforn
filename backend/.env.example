MONGO_URI=your_atlas_connection_string
PORT=5000
JWT_SECRET=replace_with_a_long_random_string
JWT_EXPIRES_IN=7d
# Anthropic API key, used by /api/ai/suggest. Leave unset to run without AI
# suggestions — the route falls back to { suggestions: [], fallback: true }.
AI_API_KEY=
